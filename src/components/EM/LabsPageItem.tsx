import React from 'react';
import styles from "./LabsPage.scss";

interface IProps {
    item: number,
    key: number,
    onChangeLab: (item: number) => void,
}

export default ({item, key, onChangeLab}: IProps) => {

}